# Project Log 规范

本目录是项目的开发知识库。与主 README（面向用户/客户）不同，这里面向开发者和 AI 助手。

---

## 文件索引

| 编号 | 文件 | 说明 | 更新频率 |
|------|------|------|----------|
| 00 | `00-project-overview.md` | 项目概述：背景、目标、技术栈 | 低 |
| 01 | `01-function-design.md` | 功能设计：模块划分、业务逻辑 | 中 |
| 02 | `02-database-design.md` | 数据库设计：表结构、关系 | 中 |
| 03 | `03-api-design.md` | API 设计：接口定义、请求响应格式 | 中 |
| 04 | `04-project-architecture.md` | 项目架构：系统架构、目录结构 | 低 |
| 05 | `05-current-status.md` | 当前状态：进度、待办、已知问题 | 高 |
| 06 | `06-dev-log.md` | 开发日志：每轮开发的记录 | 高 |
| 07 | `07-deployment.md` | 部署：部署步骤、环境要求 | 低 |
| 08 | `08-env-config.md` | 环境配置：环境变量、第三方服务 | 低 |
| 09 | `09-external-api-reference.md` | 外部服务参考：第三方 API 文档、模型列表 | 低 |
| 10 | `10-planning-log.md` | 决策记录：重要决策及备选方案 | 中 |
| 11 | `11-code-review-log.md` | 代码评审记录：多人/AI 多轮独立评审流程 | 中 |

> 编号 00-11 是标准文件。项目可根据需要自行添加 12+ 的文件。

---

## 快速查找

新成员 / 新对话按以下优先级阅读：

1. **`05-current-status.md`** — 当前进展和待办
2. **`00-project-overview.md`** — 项目背景和目标
3. 按需：

| 我想... | 看这个 |
|----------|--------|
| 了解某个功能怎么实现 | `01-function-design.md` |
| 了解数据怎么存储 | `02-database-design.md` |
| 查看 API 接口定义 | `03-api-design.md` |
| 理解代码结构和设计模式 | `04-project-architecture.md` |
| 查看最近做了什么 | `06-dev-log.md` |
| 部署到服务器 | `07-deployment.md` |
| 配置环境变量 | `08-env-config.md` |
| 查找外部 API 文档 | `09-external-api-reference.md` |
| 理解决策背景 | `10-planning-log.md` |
| 做代码评审、记录发现的问题 | `11-code-review-log.md` |

---

## 每次开工前必读 / 必做流程

每个新成员、新 AI 对话、新开发轮次开始前，先按以下流程同步上下文：

1. 读取 `05-current-status.md`，确认当前阶段、待办、阻塞和下一步。
2. 读取与任务相关的设计文档，例如功能改动读 `01`，数据库改动读 `02`，API 改动读 `03`，架构改动读 `04` 和 `10`。
3. 改代码前先说明计划、影响范围和可能需要同步更新的 project-log 文件。
4. 涉及较大改动时，先在 `10-planning-log.md` 记录方案并确认，再动代码。
5. 改完后更新 `05-current-status.md` 和 `06-dev-log.md`。
6. 涉及功能、数据库、API、架构、部署、环境变量或外部服务时，同步更新对应编号文档。
7. 无法验证、发现文档与代码不一致、或遇到不确定决策时，必须明确写出，不要默认跳过。

### 任务交接格式

多人或多个 AI 接力时，在 `05-current-status.md` 中保留一段交接信息：

```markdown
## 任务交接

**当前任务**：
**已完成**：
**未完成**：
**下一步建议**：
**风险 / 阻塞**：
**相关文件**：
```

---

## 文档哲学：只增不删，改动留痕

- **只增不删** — 过时内容划掉并注明原因，不直接删除
- **记录「改动 + 原因」** — 不只写结果，要写为什么这么做
- **保留被否定的方案** — 「为什么不用 X」和「为什么用 Y」同样有价值
- **不适用也要标注** — 某节暂时不用时，写明「不适用」和原因，不直接删除结构

修改已有内容的方式：

```
<!-- 旧方案（废弃于 2026-05-07，原因：性能不足） -->
~~使用 SQLite 作为主数据库~~

<!-- 新方案 -->
使用 PostgreSQL，详见 10-planning-log.md
```

### 决策流程

较大改动（新功能、架构调整、技术选型变更）前，先在 `10-planning-log.md` 中写清楚方案和决策依据，确认后再动代码。每条决策记录包含：决策编号（如 ADR-001）→ 状态 → 背景与需求 → 采用的方案 → 备选方案及否定原因 → 决策依据 → 改动范围。

决策状态建议使用：

| 状态 | 说明 |
|------|------|
| 提议中 | 方案还在讨论，不能作为已确定依据 |
| 已采用 | 已确认并按此执行 |
| 已废弃 | 曾经采用或讨论过，但后续不再使用 |
| 被替代 | 被新的决策记录替代，需要注明替代编号 |

---

## 开发日志格式

每轮开发在 `06-dev-log.md` 中追加一条记录：

```markdown
## YYYY-MM-DD（主题描述）

**触发原因**：这次工作的起因是什么。

**修改内容**：
1. 文件路径 — 改了什么、为什么改
2. ...

**遇到的问题**：
- 问题描述

**解决方式**：
- 如何解决的

**验证方式**：
- 运行了什么测试、命令，或做了什么手动检查

**验证结果**：
- 通过 / 未通过 / 未运行及原因
```

---

## Git Commit 格式

```
<type>: <简短描述>
```

| 类型 | 用途 |
|------|------|
| `feat` | 新增功能 |
| `fix` | Bug 修复 |
| `refactor` | 重构（不改变外部行为） |
| `docs` | 文档更新 |
| `style` | 样式/格式调整（不影响逻辑） |
| `chore` | 构建、依赖、配置等杂项 |

每次提交对应一个清晰的意图。如果一次改动包含多个独立目的，拆成多个提交。

---

## 开发规范

以下规则对人、AI 都适用：

- **只改被要求改的地方**。不要顺手"优化"周边代码——未经要求的改动会引入未预期的风险，也让 diff 难以审查。
- **发现需要额外修改时先提出来**，不要夹在同一轮改动中。
- **不要删除注释或文档注释**，除非它们明显错误或有误导性。
- **新增文件或功能模块时**，同步更新对应的 project-log 文档。

### AI 特别提醒

1. **动手前先理解** — 读懂相关代码和文档后再改，不要基于猜测修改
2. **改动范围克制** — 只做被要求的事，不主动重构、清理、添加"改进"
3. **同步更新文档** — 涉及功能变更、数据库改动、API 新增，同步更新对应文档
4. **遇到矛盾先说出来** — 发现代码或文档有问题，先指出让人确认，不要悄悄改掉
5. **遇到文档不确定时** — 以代码为准，但要告知用户文档与代码的差异，建议更新

### 安全与敏感信息

- 不要把真实 token、password、private key、cookie、连接串密码写入 project-log。
- 示例值必须使用假值，例如 `sk-xxx`、`example-password`、`postgresql://user:password@localhost/dbname`。
- `.env.example` 只记录变量名和占位示例，不记录真实密钥。
- 如果发现敏感信息曾被提交或写入文档，在 `06-dev-log.md` 记录处理方式，并立即轮换对应密钥。

---

## 主 README 与 project-log 的分工

| 内容 | 主 README | project-log |
|------|-----------|-------------|
| 项目简介 / 功能介绍 | ✅ 写 | ❌ 不写 |
| 快速开始 / 安装指南 | ✅ 写 | ❌ 不写 |
| 技术栈概览 | ✅ 简要列出 | ✅ 详细说明 |
| 数据库设计 | ❌ 不写 | ✅ `02` |
| API 详细定义 | ❌ 不写 | ✅ `03` |
| 架构决策 | ❌ 不写 | ✅ `04` + `10` |
| 开发日志 | ❌ 不写 | ✅ `06` |
| 部署详细步骤 | ❌ 不写 | ✅ `07` + `08` |
| 当前进度 | ❌ 不写 | ✅ `05` |
| 代码评审记录 | ❌ 不写 | ✅ `11` |

**原则**：主 README 给「外人」看，project-log 给「自己人」看。
