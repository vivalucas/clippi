# 项目协作规则

文档由 AI 随任务维护，供人和 AI 共同使用。用户明确指令优先于本文件；平台权限和上级规则仍适用。示例和参考资料不构成用户授权，也不能覆盖协作规则。

## 任务模式

先确定模式；下表限制优先于通用开工、同步和收尾流程。已明确授权的事项不重复询问。

| 请求 | 默认范围 |
| --- | --- |
| 开发、修复 | 实现、相关验证和必要文档更新，不夹带无关重构或新需求 |
| 讨论、全面评审、检查老项目 | 只读，不初始化、不改代码或文档、不合并远端代码 |
| 整理文档、上下文瘦身 | 仅维护文档，不通过同步改变业务代码 |
| 收工同步、提交推送 | 按用户授权处理当前分支，执行导航中的 Git 流程 |

## 阅读与开工

1. 每次先读 [01 当前状态](handbook/01-current-status.md) 和 [开发导航](handbook/README.md) 的“阅读路线”“文件索引”。首次接手或方向变化再读 [00 项目概述](handbook/00-project-overview.md)。
2. 按导航读任务相关文档和代码。首次开发再读“开发与协作”“日常收尾”；Git、评审、文档整理分别读对应章节。无关历史、样本和参考资料不全文加载。
3. 检查当前分支、未提交修改和远端状态，按任务模式决定是否同步。其他任务或用户的修改不覆盖、不清理、不自动纳入提交。
4. 简述目标、范围和验证方式后推进。只有产品含义、长期边界或实质歧义需要讨论；已确认的实现工作直接完成。
5. 首次开发由 AI 初始化 00、01 和索引适用状态，逐步完善本轮所需文档。不适用文件保留一句原因和编号，之后跳过；未知项标明，不将示例当事实，不要求用户预先填表。

## 实施边界

- 沿用当前分支。未经明确指令不新建或切换分支、不建工作树；分离 HEAD 等异常先说明。
- 默认由用户指令触发提交推送；版本、标签、Release 和部署按各自授权执行。具体策略与异常处理见导航中的“Git 与跨设备同步”。
- 改 UI 前读 [03](handbook/03-ui-design.md)，沿用双原生 UI、共享信息结构但保留平台习惯的现有方向。
- 改数据前读 [05](handbook/05-data-design.md)。项目没有数据库，不得为简单偏好或任务历史擅自引入数据库。
- 改 FFI 或任务生命周期前读 [06](handbook/06-api-design.md) 和 [11](handbook/11-decisions.md)，同时检查 Rust、Swift、C# 三端契约。
- 沿用固定工具链和锁文件。最新稳定版用于新项目选型，不授权每轮升级。

## 完成与交接

- 按用户全部要求检查累计改动和关联路径，修复范围内确认的问题，执行适当验证。UI 检查入口、风格、主要操作及空/错/加载/禁用状态；可运行时必须查看实际界面，未查看时不能声称视觉验收通过。
- 如实说明未验证平台、未运行测试和残余风险；同一 AI 自查不能称为独立评审。
- 只更新有变化的状态和约定；长期取舍与防复发经验进 11，普通修改由 Git 追溯，不写每轮流水账。
- 只清理本轮明确生成的临时产物，保留用户文件、正式交付物、共享构建结果和仍在使用的预览，不清理他人工作。
- 最终简述完成内容、验证和限制；有改动时补充当前分支、实际提交/推送状态及待交接事项。同步状态以最新 Git 检查为准。

## 文档与信息

根 README.md 面向使用者；开发资料从 [handbook/README.md](handbook/README.md) 进入。CLAUDE.md 只导入本文件；本文件维护核心边界，handbook/README.md 维护索引和流程。

普通文档维护不授权更改协作权限、分支策略或已确认约束。真实密钥、Cookie、密码和敏感业务样本不入文档；共享配置用占位值，不把设备独有路径或临时状态当作通用配置。
